// Packages
import 'package:flutter/material.dart';

// Screens Colors
const ingredientColor = Colors.deepOrange;
const shoppingListColor = Color.fromRGBO(250, 240, 215, 1.0);
const recipeColor = Colors.deepPurple;

final List colorList = [
  ingredientColor,
  shoppingListColor,
  recipeColor,
];

// BG Colors

final bgBlack = Colors.black;
final bgWhite = Colors.white;

// util
final hoverGray = Colors.grey;
final favPink = Colors.pinkAccent;
final archiveBlue = Colors.lightBlue;
final deleteRed = Colors.red;